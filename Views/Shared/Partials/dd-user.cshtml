@using RobotJester.Models
@inject LoggedInUserService userService  
<ul class="nav navbar-nav navbar-right">
    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Welcome, @userService.LoggedInDood.first_name <span class="glyphicon glyphicon-user"></span></a>
        <ul id="login-dp" class="dropdown-menu">
            <li>
                <div class="row">
                    <div id="dd-options">
                        <div class="text-left">                        
                            <span class="glyphicon glyphicon-cog"></span> <a asp-controller="Account" asp-action="Manage">Your Account</a>
                        </div>
                        <div class="text-left">
                            <span class="glyphicon glyphicon-cog"></span> <a asp-controller="Account" asp-action="CartView">Your Carts</a>
                        </div>
                        <div class="text-left">
                            <span class="glyphicon glyphicon-cog"></span> <a asp-controller="Account" asp-action="Manage">Your Orders</a>
                        </div>
                    </div>
                    <div class="bottom text-center">
                        <form asp-controller="Account" asp-action="Logout">
                            <input type="submit" class="btn btn-sm btn-warning" value="Logout!">
                        </form>
                    </div>
                </div>
            </li>
        </ul>
    </li>
</ul>